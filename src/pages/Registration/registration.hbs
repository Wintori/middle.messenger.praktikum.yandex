<main class="container">
    <div class="modal-overlay">
        <div class="modal">
            <div class="registration">
                <form class="registration__form">
                    <h1>{{title}}</h1>
                    {{{inputEmail}}}
                    {{{inputLogin}}}
                    {{{inputName}}}
                    {{{inputSurname}}}
                    {{{inputPhone}}}
                    {{{inputPassword}}}
                    {{{inputPasswordRepeat}}}

                    {{{buttonLogin}}}
                    {{{buttonRegistration}}}
                </form>
            </div>
        </div>
    </div>
</main>