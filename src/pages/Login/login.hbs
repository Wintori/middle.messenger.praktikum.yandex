<main class="container">
    <div class="modal-overlay">
        <div class="modal">
            <div class="login">
                <form class="login__form">
                    <h1 class="login__title">{{title}}</h1>
                    {{{inputLogin}}}
                    {{{inputPassword}}}

                    {{{buttonLogin}}}
                    {{{buttonRegistration}}}
                </form>
            </div>
        </div>
    </div>
</main>